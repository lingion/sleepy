<!--
  提 PR 前请确认:
  - 测试全绿: ./gradlew :app:testDebugUnitTest && ./gradlew :app:lintDebug
  - 一个 PR 一个主题; 教务适配请用 adapt/ 分支并附采集包依据
  - 关联 issue 用 "ref #N" (维护者合并后统一关闭)
-->

## 动机 / Motivation

<!-- 为什么改? 关联 issue: ref #N -->

## 改动内容 / What changed

<!-- 改了什么, 用户可见的行为变化是什么 -->

## 测试 / Testing

<!-- 跑了哪些测试, 新增了哪些用例; 教务适配注明 fixture 与解析依据 -->

- [ ] `./gradlew :app:testDebugUnitTest` 全绿
- [ ] `./gradlew :app:lintDebug` 无新增错误
