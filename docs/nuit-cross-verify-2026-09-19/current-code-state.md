# Current Code State

- `JwNuitParser` handles Wisedu `datas[]` course objects.
- `classDateAndPlace` is split into schedule entries and expanded into one normalized course per teaching week.
- The WebView route retrieves the current term code and then the student course response.
- NUIT is routed independently from the NEU `arrangedList` parser.
- The committed fixture is redacted/synthetic; it is not a live capture.
