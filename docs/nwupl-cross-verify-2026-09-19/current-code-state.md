# Current Code State

- `JwClassicEamsParser`: existing `TaskActivity` parser; unit count is page-derived.
- WebView fallback capture path already handles classic EAMS HTML.
- This adaptation adds the school URL and a redacted fixture; no new fetch endpoint is needed.
