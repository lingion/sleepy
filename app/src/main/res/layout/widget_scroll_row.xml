<?xml version="1.0" encoding="utf-8"?>
<!-- 条带行 v3 (2026-09-10 第三次实现)。
     多 child 48dp 切片 → launcher ListView extent 冻结 (ceil(V/H)*H−V 实测吻合),
     尾部不可达; 单 child 自适应行高 → OPPO launcher 量出错高
     (626px 等比高量成 270px) → fitXY 压扁 + 渲染错乱。
     v3 契约: 单 child 整张长图, 行高由服务端 setViewLayoutHeight (API31+) 按
     位图真实 dp 显式钉死 — launcher 无量测自由; 固定 dp 兜底值只服务
     API<31 (真机不在列) 与 fallback, 绝不做自适应量测 (量错高根因)。
     v9.3 API26-30 退化声明: setViewLayoutHeight 是 API31+ 独占 (minSdk 26) —
     低版本 launcher 行高停在本兜底值。v9.3 分页后每页位图高 = widget 视口高
     (随实例尺寸变化), API<31 上固定 386dp 行高无法跟随每页位图 (顶部对齐显示,
     下方可能留空)。已评估的替代均不可行: match_parent 行高在 OPPO 上重现 v2
     量错高翻车; RemoteViews 行内无其他 per-row 高度 API。接受降级, 在此留档
     不静默。 -->
<ImageView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_row_bitmap"
    android:layout_width="match_parent"
    android:layout_height="386dp"
    android:scaleType="fitCenter" />
