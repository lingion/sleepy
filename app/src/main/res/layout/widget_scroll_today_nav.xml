<?xml version="1.0" encoding="utf-8"?>
<!-- Today widget overflow container with pinned real-view header (issue #24, v3 2026-09-10).
     v2 壳图层已删: 整图行 (emptyHeader 长图) 自带 20dp 圆角背景 + 全部内容,
     壳 = 静止底图 → 滚动时"两个图层在移动"的重影根因, 零视觉贡献。
     z-order: ListView 在下, 顶栏真实视图在上。顶栏不透明背景 (运行时
     setBackgroundColor, 同 scheme) 挡住上滑内容于 36dp 线; 长图滚动位 0
     保留 24dp 头部前进量 → 顶栏下方即首行课程, 与静态渲染坐标一致。 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_scroll_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_scroll_clip"
    android:outlineProvider="background"
    android:clipToOutline="true">
    <ListView
        android:id="@+id/widget_strip_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:scrollbars="none"
        android:cacheColorHint="@android:color/transparent"
        android:listSelector="@android:color/transparent" />
    <!-- Pinned header on top of ListView: opaque bg (runtime) blocks scrolled
         rows; whole-image at scroll 0 has blank 38dp header zone (emptyHeader=true)
         so header views overlay cleanly. -->
    <LinearLayout
        android:id="@+id/widget_today_header"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="10dp"
        android:paddingEnd="10dp">
        <TextView
            android:id="@+id/widget_today_nav_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="13sp" />
        <ImageView
            android:id="@+id/widget_today_nav_prev"
            android:layout_width="40dp"
            android:layout_height="28dp"
            android:layout_marginStart="4dp"
            android:clickable="true"
            android:contentDescription="@string/today_nav_prev_day" />
        <LinearLayout
            android:id="@+id/widget_spacer_l"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />
        <TextView
            android:id="@+id/widget_today_nav_today"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="6dp"
            android:textStyle="bold"
            android:textSize="11sp" />
        <LinearLayout
            android:id="@+id/widget_spacer_r"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />
        <ImageView
            android:id="@+id/widget_today_nav_next"
            android:layout_width="40dp"
            android:layout_height="28dp"
            android:clickable="true"
            android:contentDescription="@string/today_nav_next_day" />
    </LinearLayout>
</FrameLayout>
